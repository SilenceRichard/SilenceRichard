const owner = "SilenceRichard";
const repo = "SilenceRichard";
// TODO
const token = process.env.PAT;
const workflow_id = "update-followers.yml";

module.exports = {
  owner,
  repo,
  token,
  workflow_id
}